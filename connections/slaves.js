/** Handle the connections from slaves (other synchronode instances serving iles) */
function slaveConnections(arguments) {
    arguments.Logger.log("to implement: listening for slaves");
}

module.exports = slaveConnections;