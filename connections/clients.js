/** Handle the connections from clients (via browser) */
function clientConnections(arguments) {
    arguments.Logger.log("to implement: listening for clients");
}

module.exports = clientConnections;