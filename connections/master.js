/** @module master
 * Handle the connections with the server */

function connectMaster(arguments) {
    arguments.Logger.log("to implement: connection to master");
    // get token and put it into the store
}

module.exports = connectMaster;